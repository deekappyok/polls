<!DOCTYPE html>
<html lang="pl-PL">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Simple Polls 💜 Results - <%= poll.name %></title>

    <link rel="stylesheet" href="/css/globals.css">
    <link rel="stylesheet" href="/css/index-style.css">
</head>
<body>

    <section class="index">
        <div class="index-box">
            <h1>Results  <span style="opacity: 75%;">(<%= votes %> votes)</span></h1>
            <p><%= poll.name %></p>
            <table>
                <tr>
                    <td>Option</td>
                    <td>Votes</td>
                </tr>
                <% for(let i=0; i<poll.options.length; i++) { %>
                    <tr>
                        <td class="index-table-title"><%= poll.options[i].name %></td>
                        <td class="index-table-title"><%= poll.options[i].votes %> (<%= poll.options[i].percentage %>%) </td>
                        <td class="index-table-title" style="width: 100px; padding-right: 25px;">
                            <div class="progress">
                                <div class="bar" style="width: <%= poll.options[i].percentage %>%;"></div>
                            </div>
                        </td>
                    </tr>
                <% } %>

                <a href="/polls/<%= poll.id %>">Vote</a>
            </table>
        </div>
    </section>
</body>
</html>